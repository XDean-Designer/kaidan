# PRD：开单结账（散客开单 · 会员开单 · 快速消费 · 商户端）

| 字段 | 内容 |
|------|------|
| 文档版本 | v1.0 |
| 日期 | 2026-08-20 |
| 交互原型（唯一可视依据） | 同目录 `index.html` |
| 画布 | 390 × 844 |
| 目的 | 供前端 / 后端 / 测试及 AI 工具落地「开单结账」；**只需本文 + 交互原型** |
| 关联（非必读） | 同目录 `card/PRD-项目创建与管理.md`（价目表为添加项目/产品的数据源；结算页以线上现有流程为准） |

---

## 怎么读这份 PRD

正文按模块编排。**不用通读全文**，按角色跳章即可。

| # | 模块 | 前端 | 后端 | 测试 | 说明 |
|---|------|:----:|:----:|:----:|------|
| 1 | 背景 / 目标 / 范围 / 能力映射 | ✓ | ✓ | ✓ | 本期变更叙事、防范围蔓延 |
| 2 | 用户场景与成功标准 | ✓ | 了解 | ✓ | 测「业务对不对」 |
| 3 | 信息架构与页面清单 | ✓ | 了解 | ✓ | 做哪些页、哪些层 |
| 4 | 状态机 | ✓ | **必看** | **必看** | 顾客身份、性别、会员卡展开、折扣档位、购物车计数 |
| 5 | 交互细则 / 异常流 | ✓ | 了解 | ✓ | 输入规则、校验、toast |
| 6 | 功能规格 | ✓ | 了解 | ✓ | 逐屏字段与跳转 |
| 7 | 数据模型与接口约定 | ✓ | **必看** | ✓ | 沿用线上接口；新增字段【待联调确认】 |
| 8 | 业务规则 | ✓ | **必看** | ✓ | 金额口径、折扣吸附、范围边界 |
| 9 | 文案清单与权限 | ✓ | 部分 | ✓ | 文案；店员/店长/店主 |
| 10 | 验收 P0 + 造数场景 | 了解 | 了解 | **必看** | Given→When→Then，含输入边界 |
| 11 | 非功能（极简） | ✓ | ✓ | ✓ | |
| 12 | 交付物 | ✓ | — | ✓ | 仅 PRD + 原型 |

**先读这几条（贯穿全文）**

1. 本期是基于 **RTB 线上开单结账** 的 **优化改版**，**不改变其他未涉及的功能逻辑**。凡本文未单独描述的交互、文案、接口，均沿用线上现有实现。
2. 页面顶部顾客卡片变化：男女头像 **更换为新素材**；点击头像可切换散客性别，头像右侧显示「男散客 / 女散客」；「选择顾客」进入顾客列表，列表新增「男散客 / 女散客」选项，列表内 **VIP 标签样式做了微调**。
3. 从顾客列表选择 **会员** 后，开单结账页顶部卡片切换为 **会员样式**。原会员信息卡默认 **折叠** 会员所持权益，本期改为 **保持展开、展示全部持有权益且不可折叠收起**；字段显示规则维持现有规则。**会员姓名右侧的 VIP 标签只是原型展示用的临时资源**，开发时保持该位置现有 VIP 标签逻辑不变。
4. 添加项目 / 产品时的 **UI UX 是本期最大的修改**：参照博卡小店通的 sheet 弹窗样式，按现有逻辑做了适配；设计了 **独特的选择员工 UI UX**（点击卡片翻转成按钮进行相关选择）；设置折扣采用 **拟物标尺组件**。购物车添加项后，点击「下一步」进入 **原有的开单结账结算页**。
5. **新增「快速消费」功能**：无需常规流程的繁琐操作，**只需输入金额即可完成开单结算**，并 **可选填** 服务员工信息和备注。为线下调研后决定加入的重要功能。
6. 该模块的 **其他功能保持现有状态**（挂单、结算、支付、会员卡业务等均以线上为准）。
7. 输入与联动口径（原型已同步）：金额格式 **整数≤6位、小数≤2位、上限 999999.99**；「实收」可手动编辑并 **反推折扣，吸附到最近 0.5 折档位**；快速消费 **空金额不可「去收款」**。

交互行为与文案以 **`index.html` 当前表现** 为准。

---

# 模块 1　背景 / 目标 / 范围 / 能力映射
> 读者：前端 ✓ · 后端 ✓ · 测试 ✓

## 1.1 背景

RTB 线上开单结账经过一段时间运营，线下调研反馈：

- 常规开单需先选顾客、选项目、配员工、设折扣、填备注，流程较长，存在「只想快速收一笔钱」的场景；
- 顾客卡片与顾客列表在性别表达、头像素材上体验欠佳；
- 会员所持权益信息默认折叠，收银员核对权益时需要额外操作；
- 添加项目/产品时的信息组织与操作路径不符合当前门店操作习惯。

本期在不改变其他功能逻辑的前提下，对开单结账做定点优化。

## 1.2 目标

| 目标 | 说明 |
|------|------|
| 快速消费 | 仅输入金额即可开单结算，选填员工与备注 |
| 顾客卡片优化 | 新头像素材；点击头像切换散客性别；卡片同步显示性别文案 |
| 顾客列表优化 | 新增「男散客 / 女散客」散客选项；VIP 标签样式微调 |
| 会员权益可见 | 会员卡保持展开、展示全部权益，不可折叠 |
| 添加体验升级 | 参照博卡小店通 sheet 弹窗样式，四宫格联动 + 拟物折扣标尺 + 翻转选员工 |
| 回归可控 | 其余功能沿用线上，避免范围蔓延 |

## 1.3 本期做（In）

- 顶部顾客卡片：男女头像更换；点击头像切换散客性别（男散客 / 女散客）；卡片展示性别文案
- 顾客列表：新增「男散客」「女散客」选项；会员 VIP 标签样式微调（原型为渐变 ◆VIP）
- 会员卡：从默认折叠改为 **保持展开展示全部权益、不可折叠**；VIP 标签展示沿用线上逻辑
- 添加项目/产品 sheet 弹窗：标题（项目名+价格）、四宫格（数量 / 应收 / 折扣 / 实收）、服务员工（产品为销售人员）、确定
- 折扣设置：拟物标尺组件（0~10 折，0.5 折一步，免费/5折/原价标签）
- 选择员工：卡片翻转两步选择（点客/散客 → 小工/中工/大工），多选，保留已选
- 快速消费：金额键盘直接开单 + 选填员工/备注 + 去收款
- 下一步：购物车有项 → 进入原有开单结账结算页（原型 toast 提示，结算页以线上为准）

## 1.4 本期不做（Out）

- 结算页 / 支付 / 收银流水（沿用线上，原型不搭建）
- 挂单、会员办卡、储值/计次卡核销（沿用线上，原型 toast 示意）
- 购物车明细 / 移除（沿用线上现有：点击底部 BAR 购物车图标展开 sheet 展示明细）
- 顾客列表搜索 / 新增顾客 / 编辑顾客（沿用线上）
- 多门店、权限体系改造（沿用线上现有实现，本期不改造）

## 1.5 旧 → 新能力映射

| 旧体系能力（语义） | 新体系如何表现 |
|--------------------|----------------|
| 散客卡仅展示固定头像 | 双头像可点击切换性别，卡片文案跟随「男散客/女散客」 |
| 选择顾客仅会员列表 | 列表顶部新增「男散客」「女散客」散客选项 |
| 会员卡默认折叠权益 | 保持展开、全部权益可见、不可折叠 |
| 添加项目/产品信息组织分散 | sheet 弹窗：标题 + 四宫格联动 + 员工/备注 + 确定 |
| 折扣数字选择 | 拟物标尺（0~10 折，0.5 折步进） |
| 选择员工平铺点选 | 卡片翻转两步选择（点客/散客 → 工位），多选保留 |
| 无快速路径 | 快速消费：输入金额即可开单，选填员工/备注 |
| 常规开单 | 项目/产品加购 → 下一步 → 原有结算页（沿用线上） |

映射不足时以原型字段与行为为准。

---

# 模块 2　用户场景与成功标准
> 读者：前端 ✓ · 后端 了解 · 测试 ✓

## 2.1 角色

| 角色 | 说明 |
|------|------|
| 店员 | 日常收银开单 |
| 店长 | 日常收银开单、门店管理 |
| 店主 | 门店最高权限 |

本期 **仅列角色**，**不定义角色间权限差异**；操作权限沿用线上现有规则。

## 2.2 核心场景

| 场景 | 用户期望 | 成功标准 |
|------|----------|----------|
| 散客开单 | 新客不建档案直接开单 | 顶部显示散客卡；可切换性别；加购项目/产品后下一步进入结算 |
| 会员开单 | 老客识别身份、查看权益 | 选择会员后卡片切换为会员样式；权益全部展开可见 |
| 快速消费 | 只需输入金额即可收银 | 输入金额 + 选填员工/备注 → 去收款 |
| 更换顾客 | 开单中换人 | 顶部卡片、身份状态同步切换 |
| 添加项目 | 加购服务并指定员工、折扣 | sheet 弹窗四宫格联动正确；折扣标尺可设；员工可多选 |
| 下一步结算 | 进入既有结算流程 | 购物车有项进入结算页（原型 toast 提示） |

---

# 模块 3　信息架构与页面清单
> 读者：前端 ✓ · 后端 了解 · 测试 ✓

```
开单结账
 ├─ s1 散客开单
 │    ├─ 顶部散客卡（双头像·性别文案·选择顾客）
 │    ├─ 项目/产品 Tab + 搜索 + 快速消费行
 │    ├─ 左侧分类（选中白底）+ 项目/产品卡片（圆形加号）
 │    └─ 底部 hint-bar + bottom-bar（购物车/应付/挂单/下一步）
 ├─ s2 快速消费
 │    ├─ 顶部身份卡（散客/会员 按身份切换）
 │    ├─ 收款金额 / 员工 / 备注
 │    └─ 数字键盘 + 去收款
 ├─ s3 快速消费去收款
 │    ├─ 顶部身份卡（同上）
 │    ├─ 收款金额（只读显示，键盘可改）/ 员工 / 备注
 │    └─ 数字键盘 + 去收款
 ├─ s4 选择顾客
 │    ├─ 搜索栏 + 取消
 │    ├─ 散客（男散客 / 女散客）
 │    ├─ 会员列表（姓名 / VIP 标签 / 编号 / 最近消费）
 │    └─ 字母索引 A-Z#
 ├─ s5 会员开单
 │    ├─ 顶部会员卡（权益全部展开）
 │    ├─ 项目/产品 Tab + 搜索 + 快速消费行
 │    ├─ 左侧分类 + 项目/产品卡片（圆形加号）
 │    └─ 底部 bottom-bar（购物车/应付/挂单/下一步）
 └─ 弹层
      ├─ addsheet 添加项目/产品（四宫格 + 员工 + 确定）
      ├─ ruler-sheet 设置折扣（拟物标尺 + 确定）
      ├─ staff-sheet 选择员工（网格 + 两步翻转 + 完成）
      └─ remark-sheet 备注（textarea 200 字 + 完成）
```

### 3.0 页面流（示意）

```mermaid
flowchart LR
  S1[散客开单] -->|快速消费| S2[快速消费]
  S1 -->|选择顾客| S4[选择顾客]
  S2 -->|去收款| S3[快速消费去收款]
  S4 -->|选男/女散客| S1
  S4 -->|选会员| S5[会员开单]
  S5 -->|选择顾客/更换顾客| S4
  S1 -->|加购| AS[[添加项目/产品]]
  AS -->|设折扣| RS[[设置折扣]]
  AS -->|选员工| SS[[选择员工]]
  S2 -->|备注| RM[[备注]]
  S1 -->|下一步(有项)| PAY[原有结算页]
  S5 -->|下一步(有项)| PAY
```

| 页面/层 | 导航标题（原型） | 说明 |
|---------|------------------|------|
| 散客开单 s1 | 开单结账 | 散客卡 + 项目/产品选品 + 底部工具栏 |
| 快速消费 s2 | 开单结账 | 金额键盘 + 选填员工/备注 + 去收款 |
| 快速消费去收款 s3 | 开单结账 | 金额显示（键盘可改）+ 员工/备注 + 去收款 |
| 选择顾客 s4 | — | 搜索栏 + 散客/会员列表 + 字母索引 |
| 会员开单 s5 | 开单结账 | 会员卡（权益展开）+ 项目/产品选品 + 底部工具栏 |
| 添加项目/产品 | — | sheet 弹窗（标题/四宫格/员工/确定） |
| 设置折扣 | 设置折扣 | 拟物标尺 + 当前值 + 确定 |
| 选择员工 | 选择员工 | 员工网格 + 两步翻转选择 + 完成 |
| 备注 | 备注 | 文本框（200 字）+ 完成 |

---

# 模块 4　状态机
> 读者：前端 ✓ · 后端 **必看** · 测试 **必看**

## 4.1 顾客身份 `currentCust`

| 状态 | 含义 | 顶部卡片 |
|------|------|----------|
| `guest` | 散客 | 深色散客卡（双头像 · 性别文案 · 选择顾客） |
| `member` | 会员 | 会员卡（头像/姓名/VIP/上次到店/办卡/更换顾客 + 权益展开） |

```
guest ──选会员──► member
member ──选男/女散客──► guest
```

- 默认 `guest`。
- s2 / s3 顶部卡片按该状态切换；后续元素（收款金额/员工/备注）随卡片高度下移。
- 从 s4 选会员：回到来源页（s2/s3 返回原页；从 s1 进入则跳 s5）。
- 从 s4 选男/女散客：回到来源页（从 s5 进入则回 s1）。

## 4.2 散客性别 `data-g`

| 状态 | 卡片文案 | 头像表现 |
|------|----------|----------|
| `male`（默认） | 男散客 | 男头像彩色，女头像灰阶 |
| `female` | 女散客 | 女头像彩色，男头像灰阶 |

- 点击顶部卡片头像可切换性别；切换仅影响展示，不改变身份（仍为散客）。
- s4 散客列表选择「男散客 / 女散客」同样更新。

## 4.3 会员卡权益展开

- **保持展开、展示全部持有权益，不可折叠收起**（区别于线上默认折叠）。
- 字段显示规则维持现有规则不变（如：计次卡「余N次」、储值卡「余额xx元」、折扣卡「x.x折」）。

## 4.4 折扣档位 `asRulerTick`

- 0 ~ 20 档（tick），每档 = 0.5 折；`折扣率 = tick / 20`。
- 标签：tick=0「免费」；tick=10「5折」；tick=20「原价」；其余「N 折 / N.5 折」。
- 实收手动编辑后反推 tick 并 **吸附到最近档位**（见模块 8）。

## 4.5 购物车分类计数

- 按「页面 + 左侧分类组」计数红点；加购成功后 `+1`；初始为 0 且隐藏。
- 购物车明细 / 移除沿用线上（bottom-bar 购物车图标展开 sheet）。

## 4.6 添加弹窗四宫格联动

| 字段 | 输入 | 联动 |
|------|------|------|
| 数量 | 步进 1~999 | 应收 = 单价 × 数量 |
| 应收（元） | 手动可改 | 实收 = 应收 × 折扣 |
| 折扣 | 标尺（0~10 折） | 实收 = 应收 × 折扣 |
| 实收（元） | 手动可改 | 反推折扣 → 吸附 0.5 档 → 回写实收 |

---

# 模块 5　交互细则 / 异常流
> 读者：前端 ✓ · 后端 了解 · 测试 ✓

## 5.1 金额输入规则（快速消费金额 + 应收/实收统一）

| 规则 | 说明 |
|------|------|
| 整数位 | ≤ 6 位 |
| 小数位 | ≤ 2 位 |
| 上限 | 999999.99 |
| 下限 | 0（不可为负） |
| 小数点 | 最多 1 个；**禁止以「.」开头** |
| 前导 0 | 自动剔除（输入 `0` 后再输数字视为替换，如 `05` → `5`） |
| 空值 | 不可提交；快速消费空金额点「去收款」toast「请先输入金额」 |

## 5.2 实收反推折扣

- 实收输入时：实时按 `实收 ÷ 应收` 反推折扣并预览（吸附到最近 0.5 折档位文案）。
- 实收失焦 / 点确定：吸附到最近 0.5 折档位，**实收同步改写为该档位对应值**，标尺与折扣文案一致。
- 应收无效（为空/≤0）时回退：实收 = 应收默认值 × 折扣。

## 5.3 员工 / 备注（选填）

- 员工：非必填；快速消费与添加弹窗各自维护选择状态，互不影响。
- 备注：非必填；≤ 200 字，实时计数 `n/200`；空 / 全空格视为未填。

## 5.4 校验失败（toast）

| 条件 | 提示 |
|------|------|
| 快速消费空金额点「去收款」 | 请先输入金额 |
| 下一步（购物车为空） | 请先选择项目 |
| 下一步（购物车有项） | 已进入开单结账，请按现有流程完成结算 |
| 挂单 | 已挂单 |
| 底部「+」按钮 | 请选择项目 |
| 底部「+」按钮（s5 触发） | 选择项目或快速消费 |
| 办卡 | 办理会员卡 |
| s3 去收款 | 进入收款结算 |

## 5.5 弹层交互

- 遮罩点击关闭；员工 sheet 关闭（点遮罩/完成）**保留已选状态**，重进不重置。
- 添加弹窗遮罩关闭不写入购物车；「确定」后写入并 toast「已加入 · 应付 ¥x.xx」。

---

# 模块 6　功能规格
> 读者：前端 ✓ · 后端 了解 · 测试 ✓

下列字段、按钮、跳转均须与原型一致；视觉以原型为准（品牌色 `#F32F41` 等）。

## 6.1 顶部顾客卡片

**散客卡（深色卡）【本期变更】**

- 双头像（男 / 女）并排；**头像素材更换为新素材**；点击头像切换性别。
- 选中的性别头像彩色，未选中的灰阶。
- 头像右侧显示「男散客 / 女散客」。
- 右侧「选择顾客」按钮 → s4。
- s1 / s2 / s3 共用同一散客卡（默认男散客）。

**会员卡（会员身份）【本期变更】**

- 头像 / 姓名 / VIP 标签 / 上次到店 / 「办卡」「更换顾客」。
- **权益区保持展开、展示全部持有权益、不可折叠**。
- 权益行：标签（计次卡/储值卡/折扣卡）+ 名称 + 值（余N次 / 余额xx元 / x.x折）。
- **VIP 标签为原型临时展示资源，开发保持现有 VIP 标签逻辑不变**。

## 6.2 项目 / 产品 Tab 与左侧分类

- 项目 Tab 分类：洗吹 / 洗剪吹 / 剪发 / 造型 / 干洗 / 接发 / 烫染发 / 护理 / 其他。
- 产品 Tab 分类：洗护 / 染烫 / 其他。
- 分类选中态：**选中白底，未选灰底**；点击后右侧内容平滑滚动到对应分类标题；默认选中首个分类。
- 分类右侧红色数字角标：显示该分类已加购数量（初始隐藏，加购后累加）。

## 6.3 项目 / 产品卡片

- 卡片：缩略图 + 名称 + 价格（¥ 前置）+ 圆形加号。
- 点加号 → 打开添加 sheet（`addCart` / `addCartM`）。
- 数据为演示数据；开发接入价目表实时数据（沿用 `card/PRD-项目创建与管理.md`）。

## 6.4 搜索栏

- placeholder「搜索项目名称或价格」；右侧「搜索」按钮。行为沿用线上。

## 6.5 快速消费行

- s1 / s5 中「⚡ 快速消费」行；点击 → 设置身份为散客并进入 s2。

## 6.6 快速消费 s2（新功能）【本期变更】

- 顶部身份卡：散客（默认）或会员（从 s4 选会员进入时）。
- **收款金额**：数字键盘输入；placeholder「请输入金额」；格式见 5.1；空金额「去收款」拦截。
- **员工**：选填；点击 → 选择员工 sheet；回填格式「姓名·点客·大工」（多人顿号连接）。
- **备注**：选填；点击 → 备注 sheet；≤200 字。
- 数字键盘：1-9 / 0 / . / ⌫；「去收款」→ s3。

## 6.7 快速消费去收款 s3【本期变更】

- 结构同 s2；金额初始取 s2 输入值，键盘可改；员工/备注与 s2 共享同一状态。
- 「去收款」→ 进入收款结算（原型 toast 提示）。

## 6.8 选择顾客 s4【本期变更】

- 搜索栏：placeholder「输入顾客姓名/手机号/编号/备注」+「取消」。
- **散客组**：新增「男散客」「女散客」两个选项；选择后返回来源页（从 s5 进入则回 s1），并更新散客卡性别与身份。
- **会员组**：按姓氏分组（A-Z# 索引）；行含姓名 / **VIP 标签（微调为渐变 ◆VIP）** / 手机号 / 最近消费；点选会员 → 会员身份 + 返回来源页（s2/s3 返回原页，s1 进入则跳 s5）。

## 6.9 添加项目 / 产品 sheet【本期变更】

- **标题区**：项目名 + 价格（红色，两行左对齐）。
- **四宫格**：
  - 数量：步进器（默认 1，1~999）
  - 应收（元）：数字输入（默认=单价×数量）
  - 折扣：展示当前折扣文案，点击 → 设置折扣 sheet
  - 实收（元）：数字输入（默认=应收×折扣；可手动改，反推折扣）
- **员工行**：标签为「服务员工」（项目）/「销售人员」（产品）；点击 → 选择员工 sheet；未选显示「请选择」。
- **确定**：写入购物车（应付总额累加、分类角标 +1），toast「已加入 · 应付 ¥x.xx」；关闭弹层。

## 6.10 设置折扣 sheet【本期变更】

- 标题「设置折扣」+ 右侧当前值（如「原价」「5 折」）。
- 拟物标尺：0~10 折，0.5 折步进；锚点标签「免费 / 5折 / 原价」。
- 确定：写入折扣并联动实收；关闭弹层。

## 6.11 选择员工 sheet【本期变更】

- 员工网格（演示 14 人，含头像/姓名/职位）。
- **两步翻转选择**：点卡片 → 卡片翻转显示「点客 / 散客」按钮 → 选择后翻转显示「小工 / 中工 / 大工」按钮 → 完成该员工选择。
- **多选**；已选员工卡片带勾选角标；**保留已选状态**（重进不重置）。
- 已选员工在添加 sheet / 快速消费页回填。
- 底部「完成」确认。

## 6.12 底部工具栏

- hint-bar：「具体金额以结算为准」（s1）。
- bottom-bar：购物车图标 / 「应付 ¥0.00」/ 挂单 / 下一步（s1、s5）。
- **购物车图标**：展开购物车明细 sheet（**沿用线上现有实现**，本期不重做）。
- 下一步：有项 → 原有结算页（原型 toast）；无项 → toast「请先选择项目」。

---

# 模块 7　数据模型与接口约定
> 读者：前端 ✓ · 后端 **必看** · 测试 ✓

**原则：凡本文未列出的接口、字段均沿用线上现有开单结账实现。**

## 7.1 沿用线上（不新增）

| 数据 | 来源 |
|------|------|
| 顾客列表（会员 + 手机号 + 最近消费） | 线上现有顾客接口 |
| 会员权益（计次/储值/折扣卡） | 线上现有会员卡接口 |
| 价目（项目/产品/分类/价格） | 价目表接口（见 `card/PRD-项目创建与管理.md`） |
| 员工 / 工位 / 点客·散客 | 线上现有员工接口 |
| 购物车 / 结算 / 支付 | 线上现有开单结算流程 |

## 7.2 新增 / 调整（【待联调确认】）

| 字段 | 说明 |
|------|------|
| 散客性别 `gender` | 顶部卡片 / 快速消费提交时随单记录 |
| 快速消费金额 | 直接作为账单金额提交，无需关联项目 |
| 快速消费员工 | 选填，可多选，含点客/散客/工位语义 |
| 快速消费备注 | 选填，≤200 字 |

## 7.3 能力清单（路径【待联调确认】）

| 能力 | 说明 |
|------|------|
| 快速消费开单 | 金额 + 选填员工/备注 + 散客性别 → 生成账单进入结算 |
| 散客性别选择 | 列表选择 / 卡片头像点击均写入 |
| 会员卡权益展开 | 返回全部权益（不再折叠） |
| 折扣档位 | 0~10 折，0.5 步进，实收反推吸附 |
| 员工多选回填 | 「姓名·点客·大工」格式 |

## 7.4 时序（示意）

```
收银员选择散客性别/会员
  → 顶部卡片刷新（身份 + 权益）
收银员输入快速消费金额（选填员工/备注）
  → 点「去收款」
  → 校验金额非空（否则 toast「请先输入金额」）
  → 进入快速消费去收款
  → 收款结算（沿用线上）
收银员添加项目/产品
  → sheet 四宫格联动（数量/应收/折扣/实收）
  → 选择员工（可多选）
  → 确定 → 应付累加 + 分类角标 +1
  → 下一步 → 原有结算页
```

---

# 模块 8　业务规则
> 读者：前端 ✓ · 后端 **必看** · 测试 ✓

1. **身份切换**：默认散客；选会员 → 会员；选男/女散客 → 散客。s2/s3 顶部卡片随身份切换，后续元素随卡片高度下移。
2. **散客性别**：默认男散客；卡片头像点击 / 列表选择均可切换；仅影响展示与随单记录。
3. **会员卡权益**：保持展开、全部可见、不可折叠；字段显示规则维持现有。
4. **VIP 标签**：原型为临时展示资源；开发保持该位置现有 VIP 标签逻辑不变。
5. **金额格式**：整数≤6 位、小数≤2 位、上限 999999.99、禁「.」开头、前导 0 剔除、不可为负。
6. **快速消费**：空金额不可「去收款」（toast「请先输入金额」）；员工/备注选填。
7. **实收反推**：实收可手动编辑；反推折扣并吸附最近 0.5 折档位；实收改写为该档位对应值；应收无效时回退。
8. **折扣档位**：tick 0~20（0.5 折/档）；标签 免费 / 5折 / 原价。
9. **数量**：步进 1~999。
10. **员工选择**：两步翻转（点客/散客 → 小工/中工/大工）；多选；重进保留已选；回填「姓名·点客·大工」。
11. **备注**：≤200 字；空/全空格视为未填。
12. **购物车**：加购后应付累加、分类角标 +1；购物车明细/移除沿用线上（bottom-bar 图标展开 sheet）。
13. **下一步**：购物车有项 → 进入原有结算页；无项 → toast「请先选择项目」。
14. **范围边界**：其余功能（挂单、结算、支付、办卡等）均沿用线上现有逻辑。
15. **角色权限**：店员 / 店长 / 店主，仅列角色，权限沿用线上。

---

# 模块 9　文案清单与权限
> 读者：前端 ✓ · 后端 部分 · 测试 ✓

## 9.1 文案清单（须与原型一致）

**导航/按钮**：开单结账、项目、产品、搜索、快速消费、选择顾客、更换顾客、办卡、挂单、下一步、去收款、完成、确定、取消、男散客、女散客、散客、设置折扣、选择员工、备注、服务员工、销售人员、请选择、数量、应收（元）、折扣、实收（元）、原价、5折、免费。

**占位符**：请输入金额、搜索项目名称或价格、添加备注（200字以内）、请输入备注（200字以内）、输入顾客姓名/手机号/编号/备注。

**Toast**：请先输入金额、请先选择项目、已进入开单结账，请按现有流程完成结算、已挂单、请选择项目、选择项目或快速消费、办理会员卡、进入收款结算、已加入 · 应付 ¥{金额}。

**权益行**：计次卡 / 储值卡 / 折扣卡 + 名称 + 值（余N次 / 余额xx.xx元 / x.x折）。

**提示条**：具体金额以结算为准。

**分类**：项目=洗吹/洗剪吹/剪发/造型/干洗/接发/烫染发/护理/其他；产品=洗护/染烫/其他。

**演示数据**（开发接入真实数据）：项目=高级设计师洗吹+造型 ¥58.9、资深设计师|精剪+洗吹造型(不满意免单）¥98、首席设计师洗剪吹一口价 ¥138、资深设计师|人气洗剪造型 ¥216.09；产品=卡诗黑钻钥源洗发水 ¥268、欧莱雅臻萃染发膏 ¥198、资生堂fino发膜 ¥158、施华蔻羊绒脂护发素 ¥88。

**员工演示数据（14 人）**：顾清扬(店主)、林屿森(美容师)、何苏叶(店长)、阿Ken(美容师)、Lisa(美甲师)、张明(美发师)、李华(美发师)、王芳(美甲师)、陈强(美发师)、赵敏(前台)、周杰(美发师)、吴婷(美睫师)、小陈(学徒)、阿宁(学徒)。

## 9.2 权限

| 角色 | 开单结账 |
|------|----------|
| 店员 | 按线上现有权限 |
| 店长 | 按线上现有权限 |
| 店主 | 按线上现有权限 |

本期不新增权限差异；原型按默认角色路径演示。

---

# 模块 10　验收 P0 + 造数场景
> 读者：测试 **必看**

## 10.1 造数

| 场景 | 说明 |
|------|------|
| 散客 | 男散客 / 女散客 两条散客身份 |
| 会员 | 至少 1 名持多张卡的会员（计次+储值+折扣） |
| 员工 | 覆盖点客/散客与三种工位的员工 |
| 项目/产品 | 至少覆盖 2 个分类 |
| 快速消费 | 空金额 / 正常金额 两条路径 |

正式环境自备等价数据；演示数据不进产品。

## 10.2 Given → When → Then（P0）

**顾客卡片 / 顾客列表**
1. **Given** 散客开单页，**When** 点击女头像，**Then** 女头像彩色、男头像灰阶、文案变「女散客」；再点男头像恢复「男散客」。
2. **Given** 散客卡默认态，**When** 首次进入，**Then** 男散客选中（男彩女灰），文案「男散客」。
3. **Given** 散客开单页，**When** 点击「选择顾客」，**Then** 进入顾客列表，顶部有「男散客」「女散客」选项。
4. **Given** 顾客列表，**When** 选「女散客」，**Then** 返回来源页，散客卡更新为女散客。
5. **Given** 顾客列表，**When** 选会员（如陈女士），**Then** 顶部切换为会员卡；从 s1 进入时跳 s5。
6. **Given** 会员身份，**When** 点「更换顾客」选男散客，**Then** 切回散客卡（从 s5 进入则回 s1）。
7. **Given** 会员列表，**When** 查看 VIP 标签，**Then** 微调后的渐变 ◆VIP 样式正确；文案/逻辑以线上现有 VIP 标签为准。

**会员卡权益展开**
8. **Given** 会员卡，**When** 查看权益区，**Then** 全部权益展开显示（计次/储值/折扣），无折叠入口。
9. **Given** 会员卡，**When** 重复进入，**Then** 权益始终展开、不可折叠收起。
10. **Given** 会员卡，**When** 查看字段，**Then** 显示规则维持现有（余N次 / 余额xx元 / x.x折）。

**添加项目/产品 sheet**
11. **Given** 项目卡片，**When** 点圆形加号，**Then** 打开添加 sheet，标题为项目名 + 红色价格。
12. **Given** 添加 sheet，**When** 步进数量，**Then** 应收 = 单价 × 数量；实收 = 应收 × 当前折扣。
13. **Given** 添加 sheet，**When** 手动改应收，**Then** 实收按新应收 × 折扣联动更新。
14. **Given** 添加 sheet，**When** 设折扣（如 5 折），**Then** 实收 = 应收 × 0.5，折扣文案「5 折」。
15. **Given** 添加 sheet，**When** 手动改实收为任意值后失焦/确定，**Then** 吸附最近 0.5 折档位，实收改写为该档对应值，折扣文案同步。
16. **Given** 添加 sheet（项目），**When** 查看员工行，**Then** 标签「服务员工」，未选显示「请选择」。
17. **Given** 添加 sheet（产品），**When** 查看员工行，**Then** 标签「销售人员」。
18. **Given** 添加 sheet，**When** 点确定，**Then** 应付总额累加实收金额、分类角标 +1、toast「已加入 · 应付 ¥x.xx」、弹层关闭。
19. **Given** 添加 sheet，**When** 点遮罩关闭，**Then** 不写入购物车。

**折扣标尺**
20. **Given** 设置折扣 sheet，**When** 拖动标尺，**Then** 档位 0.5 折步进，端点标签「免费」「原价」、中点「5折」。
21. **Given** 设置折扣 sheet，**When** 设到 0 档，**Then** 折扣文案「免费」，实收 = 0。
22. **Given** 设置折扣 sheet，**When** 点确定，**Then** 返回添加 sheet 并联动实收。

**选择员工**
23. **Given** 选择员工 sheet，**When** 点员工卡片，**Then** 卡片翻转显示「点客 / 散客」。
24. **Given** 已选「点客」，**When** 进入下一步，**Then** 翻转显示「小工 / 中工 / 大工」。
25. **Given** 员工完成选择，**When** 查看，**Then** 卡片带勾选角标；添加 sheet / 快速消费回填「姓名·点客·大工」。
26. **Given** 员工 sheet，**When** 多选多位员工后完成，**Then** 全部回填，顿号连接。
27. **Given** 员工 sheet 关闭后再次打开，**When** 查看，**Then** 已选员工保留勾选状态不重置。
28. **Given** 员工 sheet，**When** 未选员工直接完成，**Then** 允许关闭，回填区显示「请选择」。

**快速消费**
29. **Given** 快速消费页，**When** 不输入金额点「去收款」，**Then** toast「请先输入金额」，不跳转。
30. **Given** 快速消费页，**When** 输入金额后点「去收款」，**Then** 进入去收款页并显示该金额。
31. **Given** 快速消费页，**When** 点「员工」行，**Then** 打开选择员工 sheet，选填。
32. **Given** 快速消费页，**When** 点「备注」行，**Then** 打开备注 sheet，可输入 ≤200 字，计数实时更新。
33. **Given** 去收款页，**When** 修改金额，**Then** 键盘可改，员工/备注与快速消费页共享同步。
34. **Given** 快速消费，**When** 从 s4 选会员后进入，**Then** 顶部显示会员卡；选散客后显示散客卡。

**金额格式边界**
35. **Given** 金额键盘，**When** 输入 `.5`，**Then** 以「.」开头被拒绝，不进入。
36. **Given** 金额键盘，**When** 输入 `0123`，**Then** 前导 0 剔除，结果为 `123`。
37. **Given** 金额键盘，**When** 输入 `999999.999`，**Then** 小数第 3 位被拒；总值不超过 999999.99。
38. **Given** 金额键盘，**When** 输入 `9999999`，**Then** 超上限被拒。
39. **Given** 金额键盘，**When** 输入 `58.95`，**Then** 合法显示；再输数字被拒（小数已 2 位）。
40. **Given** 添加 sheet 应收/实收，**When** 输入超过 999999.99 或 2 位以上小数，**Then** 自动截断/封顶。

**下一步 / 底部**
41. **Given** 购物车为空，**When** 点「下一步」，**Then** toast「请先选择项目」。
42. **Given** 购物车有项，**When** 点「下一步」，**Then** toast「已进入开单结账，请按现有流程完成结算」（线上：进入原有结算页）。
43. **Given** s1/s5，**When** 点底部购物车图标，**Then** 展开购物车明细 sheet（沿用线上现有实现）。
44. **Given** 任意页，**When** 点「挂单」，**Then** toast「已挂单」（线上按现有逻辑）。

---

# 模块 11　非功能（极简）
> 读者：前端 ✓ · 后端 ✓ · 测试 ✓

- 画布与动效对齐原型 390 × 844。
- 原型为静态单页，所有状态为内存模拟；刷新重置。
- 埋点：无专表；若现网统一规范另案。
- 员工/价目数据为演示资源；正式接入真实数据源。
- 本期仅单店（与线上一致）。

---

# 模块 12　交付物

| 交付 | 说明 |
|------|------|
| 本文 `PRD-开单结账.md` | 开发与测试准稿 |
| 交互原型 `index.html` | 唯一可视依据 |
| `PRD-开单结账.html` | PRD 自包含 HTML 预览版 |

开发与测试只需 **PRD + 原型**。冲突时以原型界面为准修正 PRD。
